"""Sports-channel narration adapters.

Mirrors the layout of `races/narration/` but supplies sports-shaped stat
packs and a football-coded system prompt. The TTS, mixing, mux, assembly,
and timeline modules are reused as-is from `races/narration/`.
"""
