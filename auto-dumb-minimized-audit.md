# Auto Dumb-Minimized Audit: worldbankraces

Generated by /auto-dumb-minimizer on 2026-05-03.
Mode: AUTONOMOUS — purpose and baseline inferred without user confirmation.
**Treat all verdicts as starting points for review, not final decisions.**

A prior **interactive** audit was completed earlier today
(`./post-dumb-minimized-audit.md`, 2026-05-03). That audit produced the current
`CLAUDE.md`. This autonomous re-audit re-attacks the codebase against that
baseline, focusing on:

1. cleanup-residue from the prior audit (cleanups that landed but left dead
   constants/strings behind),
2. fresh drift not surfaced by the prior audit (dead `Theme` fields, dead
   config options, dead helpers, asymmetries between channels).

## Scope

**Audited:** the entire repo source tree:
- `run.py`, `config.json`, `ideas.md`, `CLAUDE.md`
- `races/`: `pipeline.py`, `util.py`, `render/{renderer.py, theme.py, layout.py, big_movers.py}`, `sources/{base.py, world_bank.py}`, `assets/{base.py, flags.py, fonts.py}`, `narration/{script.py, stats.py, timeline.py, voice.py, mux.py, assemble.py}`
- `sportstatsraces/`: `__init__.py`, `pipeline.py`, `config.json`, `sources/fbref.py`, `assets/headshots.py`, `scrapers/{fetch_pl_seasons.py, _headshots.py}`

**Excluded:**
- `cache/` and `output/` (gitignored runtime artifacts)
- `__pycache__/` (CPython caches)
- `races/assets/music/Bar_Chart_Race_Tension_*.mp3` (binary asset)
- The five MP4s in `output/` (renders, not source)

The whole repo source is small enough (~25 source files) to audit end-to-end
without sub-scoping.

## Inferred purpose

> A flexible automated chart-race video pipeline that adapts data sources,
> chart styles, and per-video features to the data; ingests performance signals
> from YouTube/X to refine future ideas and the pipeline itself; with the end
> goal of generating passive revenue from viewership. Two channels share the
> pipeline: a **world-stats** YouTube/X channel of country-vs-country races on
> macro indicators, and a **sports-stats** channel of player-vs-player races
> on soccer statistics.

*(Inferred — but the artifact itself states this verbatim in `CLAUDE.md` so
confidence is high. The user can sharpen if needed.)*

## Inferred requirements baseline

Taken from `CLAUDE.md` (which the prior interactive audit already
confirmed); reproduced here because verdicts trace to specific items.

**Shared render engine**
1. Render value-weighted bar-chart races at 30fps with smoothstep glide.
2. Pluggable data sources (`DataSource` ABC) and asset providers (`AssetProvider` ABC).
3. Three themes: `glass_dark`, `glass_dark_drift`, `glass_dark_black`.
4. Right-side spotlight callout for non-top-N entities with notable rate-of-change.
5. Per-row sparklines, rank-change flash, rounded flag corners, header trend line.
6. Per-row rate badges and retired flagging for cumulative series (opt-in).
7. Typography control (`font_scale` + per-section overrides; aggressive simplification deferred).
8. Intro animations (back-ease-out bounce on title/rows/trend).
9. Auto-size name-box per video.

**Pipeline orchestration**
10. Per-capita transform (÷ population). World only.
11. Accumulated/cumulative transform (`cumsum`). World only.
12. Single-frame and multi-frame PNG previews.
13. `--refetch` flag.
14. Channel-aware CLI: `python run.py --channel {world|sports}`.

**World-stats data layer**
15. Fetch indicators (initially World Bank via `wbgapi`).
16. Fetch flags from CDN with 5:3 aspect normalization.

**Sports data layer**
17. Scrape per-season soccer stats with rate-limit + HTML cache; player-ID disambiguation.
18. Headshot images via CDN proxy + background-removal.

**Narration**
19. LLM scripts with stat-pack + frame-keyed event timeline.
20. One variant per beat (hook/middle/ending); on-demand re-rolls.
21. ElevenLabs TTS with per-clip SHA256 cache.
22. Background music with ducking under voice.
23. Mux narration audio onto rendered MP4.
24. Archive previous narration on every regeneration.
25. `--auto-assemble` writes `narration.json` with `meta.source="auto"`; pipeline treats this as a do-not-regenerate signal.

**Big-mover curation**
26. Extract `cache/big_movers.json`; render only events with `keep:true` when curated file is configured.

**Posting / feedback (to be built — not yet implemented)**
27. Auto-post to YouTube and X.
28. Ingest performance signals to refine ideas and pipeline parameters.

*(Inferred-but-confirmed-by-prior-audit; high confidence.)*

## Observed behaviors (50)

Numbered list of attackable units in the current code. Trivia (boilerplate
imports, framework scaffolding) is omitted.

### Render engine
- B1.  Value-weighted race + smoothstep rank glide (`renderer.py`).
- B2.  Three themes registered (`glass_dark`, `glass_dark_drift`, `glass_dark_black`).
- B3.  Theme `bar_*` fields (`bar_style`, `bar_corner_radius_px`, `bar_opacity`, `bar_backdrop`, `bar_inner_gradient`) — defined and set on every theme.
- B4.  Theme `show_sparkline` / `show_total` / `smooth_year_ticker` boolean fields — defined with defaults, never read.
- B5.  `radial`, `radial_drift`, `gradient`, and solid-color background paths in `_draw_background`.
- B6.  Right-side spotlight callout (curated + auto), with switch_ratio, fade_frames, percentile, min_screen_seconds, label, subtext, prev/active animation.
- B7.  Per-row sparkline drawn under name text.
- B8.  Rank-change flash (green up / red down).
- B9.  Rounded flag corners (`_round_image_corners`).
- B10. Header trend line with progressive draw-in + position dot + year label + start/end-year ticks.
- B11. Live header total value with magnitude formatting.
- B12. Per-row "+N/SZN" rate badge (opt-in).
- B13. Per-row "RETIRED" badge (opt-in).
- B14. Intro back-ease-out bounce on title, rows, and trend line.
- B15. `auto_size_columns` callable that fits longest ever-top-N name (used only by sports pipeline).
- B16. `RowSlot` dataclass + `VerticalLayout.compute_rows` method.
- B17. `DEFAULT_VERTICAL` constant exported and imported in `renderer.py`.
- B18. Global `font_scale` multiplier wrapping `ax.text`.
- B19. Per-section font specs (title/header/row/spotlight: `size_scale` + `weight`).
- B20. `--validate-layout` CLI flag and `validate_layout()` standalone function.
- B21. Single-frame (`--preview-frame`) and multi-frame (`--preview-frames`) PNG previews, with `auto` keyword.
- B22. End-hold-seconds: hold last frame for N seconds before MP4 ends.

### Pipeline orchestration
- B23. Per-capita transform with worldwide-per-capita-corrected trend (`Σ raw / Σ pop` precomputed and re-interpolated).
- B24. Accumulated/cumulative transform with cumsum-of-trend pairing.
- B25. Auto output-filename suffix and trend-label rewrite for per-capita / cumulative modes.
- B26. `value_scale` cfg option (e.g. Mt → t = 1e6).
- B27. `--refetch` flag with CSV cache (`race_data.csv`, `icon_ids.json`, `population.csv`).
- B28. Missing-flag warnings with iso2 hint.
- B29. `--extract-movers` → `cache/big_movers.json` review-flow.
- B30. Curated big-mover spotlight via `keep:true`.
- B31. Auto big-mover spotlight via dataset-wide `|Δ|` percentile.
- B32. Big-mover events flow into narration timeline alongside rank-1 crossovers.
- B33. Channel-aware CLI in `run.py` (`--channel world|sports`).
- B34. `import sportstatsraces` mutates source/asset registries (plug-in via package import).
- B35. Sports pipeline has its own `run()` orchestrator that skips per-capita / accumulated / narration / big-movers.

### Narration
- B36. `_generate_script` single-call full-script LLM generator (fallback path when not auto-assembled).
- B37. `generate_variants` requests 1 hook / 1 middle / 1 ending per `SECTION_COUNTS = {hook:1, middle:1, ending:1}`.
- B38. `_pad_or_trim` clamps Claude's output at hardcoded `(5, 3, 3)` in `generate_variants`.
- B39. `_SECTION_GUIDE` text instructs Claude that "Each of the 5 options" / "Each of the 3 options" must be different.
- B40. `VARIANTS_TOOL` JSON-schema descriptions for `hooks` / `middles` / `endings` say "5 options" / "3 options".
- B41. `regenerate_section` re-rolls one beat and merges into `variants.json`.
- B42. `assemble.write_narration_json` writes `meta.source="auto"` and stamps `previous_usage`.
- B43. `meta.source == "auto"` guard in pipeline.py prevents `_generate_script` regeneration over an auto-assembled script.
- B44. `_archive_narration` copies prior `narration.{json,wav}` to `narration_archive/<stamp>` on every regen.
- B45. `script_doc.get('suggested_trim')` info-print + LLM `suggested_trim` schema field.
- B46. `stat_pack` (~16 derived stats: top_share_per_decade, total_growth, longest_reign_at_1, crossovers, entries_exits, jumps, volatility, flat_windows, early_boring, first/final standings, ever_present whitelist).
- B47. ElevenLabs TTS + sha256 clip cache by `(text, voice_id, model_id)`.
- B48. ffmpeg ducked background-music mix (loop, fade-in/out, voice + music + silent base).
- B49. ffmpeg `mux_audio` (`-c:v copy`).

### World-stats data
- B50. `WorldBankSource.fetch` via `wbgapi` + `pycountry`; population fetched alongside indicator.
- B51. SSL-disable monkey-patch on `requests.get` at module import (`world_bank.py`).
- B52. `FlagProvider` via `flagcdn.com` with 5:3 PIL pad.
- B53. Orbitron auto-download from Google Fonts CDN (cached).
- B54. `display_name` overrides (~33 entries).
- B55. `format_value` magnitude scaling (K/M/B/T) with `currency`/`count`/`goals` modes.

### Sports data
- B56. `FbrefPLSource` (parquet+meta read, `top_n_keep` filter, fbref-id name disambiguation).
- B57. `HeadshotProvider` with rembg/U²-Net cutout (lazy import).
- B58. `fetch_pl_seasons.py` PL full-history scraper (firecrawl, 3.5s rate-limit, html cache, parquet writes).
- B59. `_headshots.fetch_headshot` ssref proxy via curl, with `MissingTracker`.

### Misc / config
- B60. `config.json` `assets.top_n_to_fetch: 80` — present in config.
- B61. `config.json` `render.narration.suggest_trim: true` — present in config.
- B62. `ideas.md` recommends "bump `assets.top_n_to_fetch` from 60 to ~100".

## Verdicts and reasoning

| #   | Behavior | Verdict | Rank | Lens reasoning (i–v) | Verdict reason |
|-----|----------|---------|------|----------------------|----------------|
| B1  | Race + glide | KEEP | — | (i) is the product itself | Core. |
| B2  | 3 themes | KEEP | — | (i) baseline #3 | Already trimmed by prior audit. |
| B3  | Theme `bar_*` fields (×5) | DELETE | HIGH | (i) no baseline mapping; (iii) self-sourced; (iv) no concrete failure if removed — `bar_style`/`bar_opacity`/`bar_backdrop`/`bar_inner_gradient`/`bar_corner_radius_px` are never read by the renderer (no `bar_` references outside `theme.py`); (v) not constraint-driven | Slipped past. Renderer doesn't draw bars at all (race uses flag positions on a track), so these are leftovers from an earlier bar-bar design. |
| B4  | Theme display flags (`show_sparkline`/`show_total`/`smooth_year_ticker`) | DELETE | HIGH | (i) no baseline; (iv) no concrete failure — never read; (v) not constraint-driven | Slipped past. Renderer reads `render_cfg.show_total_trend` instead; the `Theme.show_*` defaults are dead. |
| B5  | Background paths (gradient + radial + radial_drift + solid) | KEEP | — | (i) baseline #3 themes use radial / radial_drift / solid | Used by all 3 themes; gradient path is the dead branch but cheap and tied to the spec union. Negligible. |
| B6  | Spotlight callout | KEEP | — | (i) baseline #4 | In production. |
| B7  | Per-row sparkline | KEEP | — | (i) baseline #5 | Visual signature. |
| B8  | Rank-change flash | KEEP | — | (i) baseline #5 | Visual signature. |
| B9  | Rounded flag corners | KEEP | — | (i) baseline #5 | Visual signature. |
| B10 | Header trend line | KEEP | — | (i) baseline #5 | Visual signature. |
| B11 | Header total value | KEEP | — | (i) baseline #5 (extension of trend) | Visual signature. |
| B12 | Rate badge | KEEP | — | (i) baseline #6 | Sports-channel feature. |
| B13 | Retired badge | KEEP | — | (i) baseline #6 | Sports-channel feature. |
| B14 | Intro animations | KEEP | — | (i) baseline #8 | First-2s retention matters for Shorts. |
| B15 | `auto_size_columns` (sport-only) | REWORK | MED | (i) baseline #9 says auto-size is a shared engine feature; (ii) world `pipeline.py` uses `DEFAULT_COLUMNS` (fixed `name_box_right=0.310`) and never calls it; (v) constraint of pipeline ordering, not engine | Drift. World names are mostly short (≤22 chars after `display_name`), so the asymmetry hasn't hurt — but the engine claim and the world reality have diverged. Either call `auto_size_columns` from world too, or weaken baseline #9 to "available; called from pipelines that need it". |
| B16 | `RowSlot` + `compute_rows` | DELETE | HIGH | (i) no baseline; (ii) unattributed; (iii) self-sourced; (iv) no concrete failure — `compute_rows` is never called outside `layout.py`; renderer computes slot positions itself | Slipped past. Dead helper from an earlier layout design. |
| B17 | `DEFAULT_VERTICAL` import | DELETE | HIGH | (iv) no concrete failure — imported at `renderer.py:18` but never referenced in the module body (renderer constructs its own `VerticalLayout` from cfg) | Dead import. |
| B18 | `font_scale` | KEEP | — | (i) baseline #7 | Used. |
| B19 | Per-section font specs | WEAKEN | LOW | (i) baseline #7 explicitly defers simplification | Per the baseline's own deferral; revisit after the production template is locked. |
| B20 | `--validate-layout` flag | WEAKEN | LOW | (iv) preview frames catch more failures; (i) not in baseline | Demote to internal helper (matches prior-audit B14). |
| B21 | Preview frames | KEEP | — | (i) baseline #12 | Core to layout iteration. |
| B22 | End-hold seconds | KEEP | — | (iv) without it the last frame would smash-cut to black; viewers would lose the payoff | Quality bar; cheap. |
| B23 | Per-capita + worldwide trend | KEEP | — | (i) baseline #10 | Core WB mode. |
| B24 | Cumulative transform | KEEP | — | (i) baseline #11 | Core WB mode. |
| B25 | Output-filename / trend-label auto-rewrite | KEEP | — | (iv) without it filenames collide and labels lie | UX of #10/#11. |
| B26 | `value_scale` cfg option | REWORK | LOW | (i) not in baseline; (iv) plausible failure if removed (some indicators are reported in non-base units) | Either add to baseline as a documented capability or remove — current state is undocumented capability creep. |
| B27 | `--refetch` + CSV cache | KEEP | — | (i) baseline #13 | Required by the auto-loop. |
| B28 | Missing-flag warnings | KEEP | — | (iv) catches real "text-only spotlight" regressions | Cheap debug aid. |
| B29 | `--extract-movers` | KEEP | — | (i) baseline #26 | Production curation flow. |
| B30 | Curated spotlight | KEEP | — | (i) baseline #26 | Production. |
| B31 | Auto spotlight | KEEP | — | (iv) the first-pass review needs auto-mode to populate `big_movers.json` | Required for the curated flow to start. |
| B32 | Movers in narration timeline | KEEP | — | (i) baseline #19 | Frame-keyed narration depends on this. |
| B33 | Channel-aware CLI | KEEP | — | (i) baseline #14 | Already converged from prior audit. |
| B34 | Plug-in registry mutation | KEEP | — | (i) baseline #2 | Clean ABC pattern; cost is one import side-effect. |
| B35 | Sports pipeline as its own orchestrator | REWORK | MED | (i) the sports pipeline duplicates ~80% of the world pipeline's source/asset/preview/render plumbing; (v) the world-side per-capita / cumulative / narration / big-movers blocks could be gated by channel flags instead | Carries forward prior-audit B20. Not urgent (the current shape is readable), but the second sports video will tempt copy-paste again. |
| B36 | `_generate_script` single-call fallback | DELETE | MED | (i) auto-assemble is now the canonical path (baseline #25); (iv) failure if removed: someone running `--generate-narration` *without* first running `--generate-variants` + `--auto-assemble` would get an error — but that is the intended full-auto flow now, and the fallback bypasses the no-spoiler discipline that lives only in `VARIANTS_ADDENDUM`. So it's not just dead — it's actively risky. | Drift. The studio path is gone; the fallback path has lost its reason to exist and its prompts haven't kept up with the variants prompt. |
| B37 | `generate_variants` 1/1/1 | KEEP | — | (i) baseline #20 | The new shape. |
| B38 | `_pad_or_trim(...,5)` / `(...,3)` | REWORK | HIGH | (i) baseline #20 says one variant per beat; (iv) concrete failure: if Claude returns 5 hooks, the file silently keeps 5 and `--auto-assemble` picks `[0]` — no immediate breakage, but the file claims 5 variants exist; (v) constraint of forgotten constants | Cleanup-residue. Replace `5`/`3`/`3` with `SECTION_COUNTS[k]`. |
| B39 | `_SECTION_GUIDE` "Each of the 5/3 options" | REWORK | HIGH | (iv) Claude reads this as part of the prompt and is told to make multiple meaningfully-different options, but the request asks for 1 of each; the model gets contradictory instructions | Cleanup-residue. Reword to single-option phrasing or parameterize. |
| B40 | `VARIANTS_TOOL` schema "5 / 3 options" | REWORK | HIGH | (iv) tool description ships in the model context; the description is now wrong | Cleanup-residue. Reword. |
| B41 | `regenerate_section` | KEEP | — | (i) baseline #20 ("on-demand re-rolls") | Required. |
| B42 | `assemble.write_narration_json` | KEEP | — | (i) baseline #25 | Required. |
| B43 | `meta.source=="auto"` guard | KEEP | — | (i) baseline #25 | Required. |
| B44 | `_archive_narration` | KEEP | — | (i) baseline #24 | Cheap insurance. |
| B45 | `suggested_trim` print + LLM schema | DELETE | MED | (i) not in baseline; (iv) no concrete failure — pipeline only prints; nobody acts on it; LLM schema cost remains | Slipped past. Either wire it into a real action (apply trim → re-render) or drop the schema field and the print. |
| B46 | `stat_pack` | KEEP | — | (i) baseline #19 | LLM grounding. |
| B47 | ElevenLabs + clip cache | KEEP | — | (i) baseline #21 | Cost control. |
| B48 | Music duck mix | KEEP | — | (i) baseline #22 | Required. |
| B49 | mux | KEEP | — | (i) baseline #23 | Required. |
| B50 | WorldBankSource + population | KEEP | — | (i) baseline #15 + #10 | Core data layer. |
| B51 | `requests.get` SSL monkey-patch | KEEP | — | (iv) without it `wbgapi` calls fail with `SSLEOFError` on some networks | Real workaround; cheap; could be moved to a `_patch_requests()` function called from `fetch()` rather than module import, but that's polish. |
| B52 | FlagProvider | KEEP | — | (i) baseline #16 | Core asset layer. |
| B53 | Orbitron auto-download | KEEP | — | (iv) renderer requires the font registered or text falls back to default | Required. |
| B54 | `display_name` overrides | KEEP | — | (iv) without it "Korea, Rep." / "Russian Federation" / "Iran, Islamic Rep." would look unprofessional in chart-race rows | UX correctness. |
| B55 | `format_value` magnitude scaling | KEEP | — | (i) baseline #5 (numeric readability) | Visual quality. |
| B56 | FbrefPLSource | KEEP | — | (i) baseline #17 | Core sports data. |
| B57 | HeadshotProvider + rembg | KEEP | — | (i) baseline #18 | Visual quality. |
| B58 | `fetch_pl_seasons.py` | KEEP | — | (i) baseline #17 | Core sports data ingest. |
| B59 | `_headshots.fetch_headshot` | KEEP | — | (i) baseline #18 | Required. |
| B60 | `assets.top_n_to_fetch: 80` cfg | DELETE | HIGH | (i) no baseline; (ii) the comment in `pipeline.py:165-166` even admits "kept as a lower bound but no longer caps the set" — but the code never reads the option at all; (iv) no concrete failure | Slipped past. Dead config field; remove from `config.json` and from `ideas.md`'s instructions. |
| B61 | `narration.suggest_trim: true` cfg | DELETE | HIGH | (i) no baseline; (iv) no concrete failure — the code has no `cfg.get('suggest_trim')` lookup; the trim print runs unconditionally | Slipped past. Dead config field. (Pairs with B45.) |
| B62 | `ideas.md` `top_n_to_fetch` mention | REWORK | LOW | (iv) misleading instruction — tells the user to bump a config value the code ignores | Cleanup-residue from B60; delete the line. |

## Deletion hit-list (ranked)

### HIGH

- **B3:** delete `bar_style`, `bar_corner_radius_px`, `bar_opacity`, `bar_backdrop`, `bar_inner_gradient` from `Theme` and from all three theme instances. Five fields × three themes = 15 dead lines.
- **B4:** delete `show_sparkline`, `show_total`, `smooth_year_ticker` defaults from `Theme`. They're shadowed by `render_cfg` flags anyway.
- **B16:** delete `RowSlot` dataclass and `VerticalLayout.compute_rows` from `races/render/layout.py`.
- **B17:** drop `DEFAULT_VERTICAL` from the import in `races/render/renderer.py:18` (and from `layout.py` if no other consumer remains — quick grep first).
- **B38 + B39 + B40 (cleanup-residue cluster):** replace the hardcoded `5`/`3`/`3` constants in `_pad_or_trim` calls, in `_SECTION_GUIDE` text, and in `VARIANTS_TOOL` schema descriptions with `SECTION_COUNTS[name]` (or just "one option" since `SECTION_COUNTS` is `{1,1,1}`). The current state ships contradictory instructions to Claude.
- **B60:** delete `assets.top_n_to_fetch` from `config.json`. The pipeline comment on line 165 already documents that it does nothing — make the code say so by removing it.
- **B61:** delete `render.narration.suggest_trim` from `config.json`. Never read.

### MEDIUM

- **B36:** delete `_generate_script` and the fallback branch in `pipeline.py:312-319`. The auto-assemble path (`--generate-variants` → `--auto-assemble` → `--generate-narration`) is the only intended flow. Anyone reaching the fallback today gets a single-shot script that doesn't honor the no-spoiler / no-play-by-play discipline that lives in `VARIANTS_ADDENDUM`. Make `--generate-narration` require a pre-assembled `narration.json` and error otherwise.
- **B15 / B35 (rework cluster):** make `auto_size_columns` either invoked by the world pipeline too (so baseline #9 is true for both channels) or weaken baseline #9 to "the engine offers it; pipelines opt in". Same call: fold `sportstatsraces/pipeline.py` into `races/pipeline.py` with channel-aware branches (carries forward prior-audit B20).
- **B45:** delete `suggested_trim` from the LLM tool schema and the corresponding print. Either wire trim into the actual pipeline (auto-apply if `boringness > X`) or drop it.

### LOW

- **B19:** simplify per-section typography overrides once the production template is locked. (Same as prior-audit B8 — defer until N videos shipped.)
- **B20:** demote `--validate-layout` to an internal helper. (Same as prior-audit B14.)
- **B26:** either document `value_scale` in `CLAUDE.md` baseline or remove the code path. Currently undocumented.
- **B62:** delete the `ideas.md` line that tells the user to bump `assets.top_n_to_fetch` (tied to B60).

## Phase 3 proposals

### Weakenings

- **B19:** "Support typography control" → "Support a single `font_scale` global multiplier; per-section overrides are deferred." Confidence: LOW (the baseline already says this; weakening here means deleting the unused override surface in the renderer). Defer.

### Merges

- **B15 + B35 → one merged action:** "Make the shared pipeline channel-aware (`races/pipeline.py` branches on world/sports), and call `auto_size_columns` unconditionally from it." Confidence: MED. This is the same direction as prior-audit B20 — the audit caught the duplicate orchestration but didn't get the pipeline merged in this round.
- **B38 + B39 + B40 → one merged action:** "Replace all three `5`/`3`/`3` literals with `SECTION_COUNTS[k]` (or single-option phrasing) in one PR." Confidence: HIGH. They're three pieces of the same incomplete cleanup.
- **B60 + B62 → one merged action:** "Remove `top_n_to_fetch` from `config.json`, the dead-comment block in `pipeline.py:163-166`, and the `ideas.md` instruction." Confidence: HIGH.

## Survivors after Phase 3

The KEEP set is the production core: render engine (B1, B2, B5–B14, B18, B21, B22), pipeline transforms (B23–B25, B27, B28), data sources (B29–B34, B50–B59), narration (B37, B41–B44, B46–B49), and one channel-aware CLI (B33). These are what every shipped video actually needs. Approximately **38 of 50 behaviors survive** as KEEPs after the deletions above.

## 10% rule

First-pass non-KEEP count: **8 DELETE + 1 WEAKEN + 5 REWORK = 14 of 50 (28%)**. **PASS — no re-attack needed.**

(Note: the prior interactive audit logged 33%; this re-audit logs 28% because most of the prior audit's recommendations have already landed. The remaining 28% is mostly cleanup-residue and dead fields the prior audit missed.)

## Confidence notes

- **Purpose was inferred — HIGH confidence.** The artifact (`CLAUDE.md`) states the purpose verbatim, and the prior interactive audit confirmed it.
- **Baseline was inferred — HIGH confidence.** Same source, same prior confirmation. The only baseline gap I see is `value_scale` (B26) — implemented but undocumented.
- **Verdicts marked LOW rank are the most likely to be wrong** — specifically B19 and B26: typography simplification depends on whether the production template is locked (defer signal lives outside the artifact); `value_scale` may be load-bearing for a future indicator that the user has in mind from `ideas.md`.
- **B36 is the highest-risk DELETE.** The `_generate_script` fallback might still be the user's "I just want a script written" escape hatch. If so, it should be re-armed with the `VARIANTS_ADDENDUM` rules — but as it stands today, it ships an outdated prompt.
- **B15 / B35 (sports-pipeline asymmetry)** is partly a baseline question, not just a code question. The user may prefer two pipelines for clarity over one merged channel-aware pipeline — if so, weaken baseline #9 instead of changing the code.

## What this audit does NOT cover

- The two **missing capabilities** in the baseline (auto-post #27, performance-feedback loop #28). No behavior to attack yet.
- **Performance / cost** of the current code (matplotlib `FuncAnimation` render time, ElevenLabs spend). Out of scope; this is a scope-and-shape audit.
- **Prompt-engineering quality** of `SYSTEM_PROMPT` / `VARIANTS_ADDENDUM`. The size and slang lists are within the prior audit's deferred-cleanup zone (B42/B43); revisit after the production template is locked.
- **`__pycache__/` directories present in working tree** — they are correctly gitignored, so this is not a tracked-files issue. The prior audit's B44 called this out but the .gitignore fix has landed; the residue is just untracked dirs on disk and is harmless.

---

**Next step suggestion:** to anchor this re-audit (especially the dead-fields and cleanup-residue) and update `CLAUDE.md`, run:

```
/post-dumb-minimizer
```

with this audit file as input — it will let you confirm or edit the inferred fields and merge the new findings into `CLAUDE.md` and `post-dumb-minimized-audit.md`.
